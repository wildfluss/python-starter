# Python starter with pyenv Mypy Pytest and VS Code settings

Quick start:

```bash
python -m venv .venv && \
. .venv/bin/activate && \
pip install -r requirements.txt
```

and/or

```bash
pip install -e .
```

and test 

```bash
pytest
```

or run app

```bash
$ myapp 
TODO
```
